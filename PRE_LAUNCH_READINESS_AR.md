# تقرير جاهزية ركائز قبل الإطلاق

**تاريخ المراجعة:** 13 أغسطس 2026

**النطاق:** مشروع الويب في هذا المستودع (`backend` + `frontend` + PostgreSQL/Supabase)

**الفرع الذي تمت مراجعته:** `agent/web-beta-launch`
**قاعدة Supabase الإنتاجية:** رُبطت بأمان في 21 أغسطس 2026، وطُبقت 17 ترحيلًا بنجاح بعد فحص مسبق للبيانات والتحقق اللاحق.

> **قرار الأولوية:** نُكمل إعداد المشروع واختباراته وأمانه أولًا، وتؤجل إجراءات
> ZATCA الفعلية وOTP والشهادات والإرسال إلى مرحلة لاحقة بموافقة مالك المشروع.
> المرجع الملزم: [أولويات مشروع ركائز](PROJECT_PRIORITIES_AR.md).

## تحديث أمني — 13 أغسطس 2026

- أضيفت جلسات خادمية قابلة للإلغاء، وخروج يبطل الرمز فورًا، وشاشة عربية/إنجليزية
  لمراجعة الجلسات وإلغاء غير المعروف منها.
- نُقلت جلسة الويب من تخزين JavaScript إلى cookie محمي `HttpOnly/Secure/SameSite`
  مع حماية Origin وFetch Metadata، ولم يعد رمز الدخول يعاد إلى الواجهة.
- قُوّي ربط الأجهزة ببصمة HMAC وcookie غير قابل لقراءة JavaScript ومنع محاولات موزع وعمر 90 يومًا، مع إبطال مقصود
  لكل رموز الأجهزة القديمة عند الترحيل وتسجيل محاولات الربط والإلغاء.
- أُغلق التسجيل العام إنتاجيًا افتراضيًا، وأضيف تغيير كلمة مرور يبطل الجلسات الأخرى،
  وأصبح تحميل نموذج ميسر يفشل إذا لم تتوفر بصمات SRI موثقة.
- أضيف منع مؤقت موزع على قاعدة البيانات بعد خمس محاولات دخول فاشلة، مع مقارنة
  كلمة مرور وهمية للحساب غير الموجود لتقليل كشف الحسابات بفارق التوقيت.
- أضيف سجل `security_events` ملحق فقط وبصمات HMAC مستعارة، مع RLS وسحب كامل
  لصلاحيات `anon` و`authenticated` في Supabase.
- هذه مواءمة تقنية مع ECC 2-2024 وCCC 2-2024 وDCC وليست شهادة امتثال رسمية.
- قبل الإنتاج: طبّق الترحيل على Staging بعد نسخة احتياطية، اضبط
  `SECURITY_EVENT_PEPPER`، اربط السجل بتنبيه/SIEM، ثم نفّذ اختبار اختراق مستقل.

## القرار التنفيذي

| نوع الإطلاق | القرار الحالي | السبب |
|---|---|---|
| تجربة ويب مغلقة لمحل أو محلين، والدفع متوقف | **جاهز بشروط** | يلزم أولًا رفع التغييرات، نجاح CI، تطبيق الترحيلات على بيئة تجريبية، ثم اختبار شامل بالأدوار والأجهزة |
| إطلاق عام لعدة محلات | **غير جاهز بعد** | نجح ربط PostgreSQL/Supabase والترحيلات، لكن لا تزال اختبارات E2E والضغط والمراقبة التشغيلية مطلوبة |
| تشغيل مدفوعات حقيقية | **غير مسموح حاليًا** | بوابة الدفع مصممة بأمان أفضل، لكن يلزم اختبار Moyasar التجريبي الكامل والاسترجاع والتجديد والتسوية قبل تفعيل العلم |
| ادعاء التكامل الكامل مع ZATCA | **غير مسموح** | الموجود توليد QR للمرحلة الأولى فقط، وليس تكامل Fatoora للمرحلة الثانية |
| كتالوج سعودي شامل أو بحث VIN عالمي | **غير متوفر بعد** | الموجود قوالب أصناف عامة ومسارات حفظ وبحث؛ بيانات OEM/VIN المرخّصة لم تُربط بمصدر فعلي |

## ما أصبح موجودًا فعليًا

- فصل بيانات المحلات على مستوى API باستخدام `organization_id` المأخوذ من جلسة المستخدم، مع تعطيل وصول أدوار Supabase العامة إلى جداول النظام.
- تسجيل دخول يتطلب رمز المنشأة، وجلسة قصيرة نسبيًا (12 ساعة)، وكلمات مرور جديدة بطول 12 حرفًا على الأقل تحتوي حرفًا ورقمًا.
- إنشاء حساب مدير لمحل جديد، ثم دعوة الموظفين والعملاء بروابط أحادية الاستخدام تنتهي بعد 48 ساعة.
- ربط أجهزة الفروع برمز قصير ينتهي بعد 10 دقائق، ومنع محاولات موزع، وهوية جهاز تنتهي بعد 90 يومًا، مع إمكانية الإلغاء وتقييد الموظف والجهاز بالفرع نفسه.
- تسجيل اسم الموظف والجهاز والفرع في حركات الصرف والبيع والمخزون.
- إدارة الفروع وبيانات المنشأة والرقم الضريبي السعودي المكوّن من 15 رقمًا.
- بحث القطع بالاسم، رقم القطعة، الباركود، OEM، الأرقام البديلة، ورقم الهيكل للسيارات المحفوظة.
- حفظ سيارة العميل مرة واحدة وإعادة البحث برقم الهيكل دون إدخال بياناتها من جديد.
- استيراد مخزون CSV بمعاينة وتحقيق للبيانات ومنع المكرر وتسجيل عملية الاستيراد وحركات المخزون.
- كتالوج ابتدائي سعودي من 20 **قالبًا عامًا** يدخل كمسودات بلا سعر أو مخزون، ولا يُعرض للبيع قبل مراجعته وتفعيله.
- معرفة القطعة من رقم الرف أو رقم القطعة أو الباركود، مع إنذارات انخفاض المخزون ضمن الفرع المقترن بالجهاز.
- معاملات ذرية عند البيع والصرف وتعديل المخزون لمنع الرصيد السالب، وأرشفة القطعة بدل حذف تاريخها.
- إخفاء التكلفة ومكان الرف والكمية الدقيقة عن العميل، وإظهار التوفر بالفرع فقط.
- إنشاء QR للفاتورة الضريبية وفق نطاق المرحلة الأولى، ومنع إصدار فاتورة قبل اكتمال اسم المنشأة ورقمها الضريبي.
- تصميم مدفوعات يتحقق من حالة الدفعة ومبلغها وعملتها وملكيتها على الخادم، مع مراجع لمنع التكرار ومحاولات استرجاع عند فشل تنفيذ الطلب.
- بوابات تشغيل إنتاجية: CORS محدد، ترويسات حماية، تحديد المعدل، حد لحجم JSON، وفحص جاهزية قاعدة البيانات.
- CI لاختبار Node 24، الواجهة والخادم، منع ثغرات الإنتاج المتوسطة فأعلى وثغرات التطوير العالية فأعلى، سلامة شجرة الحزم، وفحص الأسرار وبناء صورة Docker موحدة للويب وAPI.
- دورة مشتريات أولية: موردون، أوامر شراء واعتماد، استلام جزئي/كامل مقيد بجهاز الفرع، منع تكرار الاستلام، تحديث تكلفة مرجّحة وحركة مخزون باسم المستلم.
- تحويلات مخزون بين الفروع: طلب إداري، شحن من جهاز المصدر، استلام من جهاز الوجهة، فرق/تالف بسبب إلزامي، وسجل حركة باسم الموظفين.

## الإصلاحات الأمنية والمنطقية المنجزة في هذه الجولة

1. منع تسجيل موظف أو عميل عامًا خارج مسار دعوة المدير.
2. منع اختيار أول مستخدم متشابه بالبريد؛ رمز المنشأة أصبح إلزاميًا عند الدخول.
3. توحيد شكل المستخدم العائد من التسجيل والدخول والدعوة وتجديد الاسم.
4. قصر تعديل الكتالوج والمخزون اليدوي على المدير.
5. قصر مأمور المستودع على مخزون فرعه في البحث والقائمة والإصدار.
6. قصر سجل فواتير البائع على فرعه، وسجل فواتير العميل على حسابه.
7. إزالة بيانات التكلفة والرف والكمية الدقيقة من استجابة العميل.
8. التحقق من ملكية الفرع والقطعة داخل المحل قبل أي تغيير.
9. جعل البيع والصرف وتحديث المخزون عمليات قاعدة بيانات ذرية ومُسجلة.
10. حماية الدفع من إعادة استخدام دفعة أو تغيير المبلغ أو المتجر أو المستخدم، وإضافة قيد فريد لمرجع الدفع.
11. تعطيل الدفع افتراضيًا في الواجهة والخادم، وعدم قبول مفتاح سري في الواجهة.
12. إبقاء التوافق الذي يدخله المدير يدويًا بحالة `unverified` بدل وصفه خطأً بأنه موثّق.
13. حذف جلسة الجهاز والدفعة المعلقة عند تسجيل الخروج.
14. منع تنفيذ مرجع الدفع نفسه بالتوازي عبر قفل PostgreSQL، وإرجاع حالة قابلة لإعادة المحاولة بدل تنفيذ مزدوج.
15. إعادة جلب حالة ميسر قبل الاسترجاع حتى لا يستند الاسترجاع إلى مبلغ قديم، وقفل تشغيل مهمة التجديد لمنع تشغيل نسختين معًا.
16. تحميل نموذج ميسر الرسمي المثبت على الإصدار `2.2.10` عند فتح الدفع فقط، مع مهلة ورسالة فشل واضحة وشبكات مدى/Visa/Mastercard.
17. إزالة JWT من تخزين JavaScript كليًا ونقله إلى cookie محمي لا يستطيع كود الصفحة قراءته، مع حذف أي رمز قديم من `localStorage` و`sessionStorage`.
18. إضافة CSP وPermissions Policy ومعرف طلب وHTTPS اختياري، ومنع خدمة الملفات المخفية، وحدود لملف CSV.
19. بناء الواجهة داخل صورة الإنتاج وخدمة `dist` فقط؛ لا تنشر SQL أو `.env` أو كود الخادم أو ملفات Git.
20. إضافة Dependabot وأداة فحص أسرار وسياسة أمن وخط أساس مبني على ECC 2-2024، دون ادعاء شهادة امتثال.
21. إضافة جلسات خادمية قابلة للإلغاء، ومنع موزع لمحاولات الدخول المتكررة، وسجل أحداث أمني ملحق فقط ببصمات مستعارة.
22. إضافة شاشة أمان عربية/إنجليزية لكل مستخدم لمراجعة جلساته وإلغاء جلسة واحدة أو جميع الجلسات الأخرى.
23. إضافة حماية CSRF بطبقات SameSite وOrigin وFetch Metadata، ورفض CORS غير الآمن أو wildcard، وإجبار HTTPS وSSL لقاعدة البيانات في الإنتاج.
24. تقوية أجهزة المحلات ببصمات HMAC ومنع محاولات موزع وانتهاء تلقائي وسجل أمني، مع إبطال رموز الأجهزة القديمة الضعيفة عند الترحيل.
25. إغلاق التسجيل العام افتراضيًا وإضافة تغيير كلمة مرور آمن يبطل الجلسات الأخرى.
26. منع تشغيل نموذج ميسر إذا لم تتطابق بصمات SRI للملفات الخارجية المثبتة.

## نتائج الفحوص المحلية

| الفحص | النتيجة | الملاحظة |
|---|---|---|
| اختبارات الخادم | **69 ناجحًا / 69** | تشمل الدفع المتكرر، العزل، الشراء، التحويلات، ZATCA، سياسة الخروج الشبكي، المراقبة، الجلسات، cookies/CSRF، وتقوية الأجهزة، وتثبيت Node 24 واتصال TLS بـSupabase |
| اختبارات الواجهة | **18 ناجحة / 18** | تشمل ميسر/SRI، CSV، المشتريات، التحويلات، لوحة الامتثال، شاشة مدير الفرع، وإدارة جلسات الحساب |
| فحص بناء جملة ملفات JavaScript للخادم | **ناجح** | جميع ملفات `src` و`scripts` و`test` |
| فحص صياغة ملفات CI وDependabot | **ناجح** | تم تحليل YAML دون أخطاء |
| تدقيق اعتماديات الخادم دون اتصال | **0 ثغرات معروفة** | النتيجة مرتبطة ببيانات التدقيق المحلية؛ يعاد Online داخل CI |
| تدقيق اعتماديات الواجهة دون اتصال | **0 ثغرات معروفة** | يعاد Online داخل CI |
| كشف أسرار صريحة في الملفات المتتبعة | **لم يُعثر على مفتاح حي** | بقيت أمثلة وهمية فقط مثل `sk_test_xxx` |
| سلامة فرق Git والمسافات | **ناجح** | `git diff --check` بلا أخطاء |
| إصلاح ذاكرة npm | **مغلق في هذه البيئة** | نجح `npm ci --ignore-scripts` للخادم والواجهة من ملفات القفل دون حذف الذاكرة العامة |
| بناء الواجهة محليًا | **ناجح على Node 24** | Vite 6.4.3: 45 وحدة، وحزمة JavaScript نحو 266.20 kB قبل gzip |
| PostgreSQL/Supabase integration | **ناجح** | اتصال مباشر مشفر بشهادة Supabase الرسمية، 17/17 ترحيلًا، وبقاء منشأتين ومستخدمين بعد الترحيل |
| Supabase Security Advisor | **0 أخطاء / 0 تحذيرات** | فُعّل RLS على سجل الترحيلات وثُبت مسار دالة حماية سجل الأحداث |
| Docker build محلي | **لم يُنفّذ** | Docker غير متاح؛ CI سيبني الصورة الموحدة من جذر المستودع ويتحقق من بناء الواجهة |
| تشغيل HTTP محلي | **محجوب ببيئة الجلسة** | بيئة العمل منعت فتح منفذ محلي (`EPERM`)؛ فُحص مسار الصحة والترويسات على مستوى الاختبار والكود، ويعاد Smoke test بعد نشر Staging |
| اختبار متصفح شامل E2E | **لم يُنفّذ** | يحتاج نشرًا تجريبيًا وبيانات اختبار معزولة |
| فحوص PR الحالية على GitHub | **تحتاج إعادة تشغيل** | النسخة الجديدة ما زالت محلية ولم تُرفع؛ لذلك نتائج الفرع البعيد لا تمثل هذه الإصلاحات |

## موانع الإطلاق المطلوب إغلاقها

## ميزة مدير الفرع — مخزون فروع المنشأة (محلية ولم تُنشر)

- أضيف دور `branch_manager` ويرتبط بفرع داخل المنشأة إلزاميًا.
- يستطيع مدير الفرع البحث برقم القطعة أو الباركود أو OEM/البديل أو الاسم، ثم رؤية الكمية الموجبة وموقع الرف في الفروع الأخرى التابعة **لنفس المنشأة فقط**.
- الشاشة للقراءة فقط ولا تمنح مدير الفرع البيع أو تعديل المخزون أو رؤية التكلفة أو الموردين أو لوحة الإدارة.
- عزل المستأجر مطبق في الاستعلام على القطعة والفرع معًا، وتوجد قيود قاعدة بيانات مركبة تمنع ربط مستخدم بفرع تابع لمنشأة أخرى.
- البحث محدود إلى 50 قطعة، ويتطلب حرفين على الأقل، ويهرب رموز البحث الخاصة، وله حد طلبات مستقل.
- يسجل سجل الأمان نوع البحث وبصمة أحادية الاتجاه للاستعلام وعدد النتائج، ولا يحفظ نص بحث القطعة الخام.
- نجحت بعد الإضافة اختبارات الخادم والواجهة والبناء محليًا، وتحقق اتصال PostgreSQL/Supabase الفعلي؛ لا يزال يلزم إنشاء بيئة Staging منفصلة وإجراء اختبارات E2E قبل الإطلاق.

### حرجة قبل أي إطلاق عام

1. **رفع التغييرات الحالية وتشغيل CI على نفس الالتزام.** لا يعتمد الإطلاق إلا إذا نجحت اختبارات Node 24، بناء Vite، تدقيق الاعتماديات، وسلامة Docker كلها.
2. **إنشاء بيئة Staging منفصلة قبل الترحيلات المستقبلية.** تؤخذ نسخة احتياطية، تطبق الترحيلات وتختبر العودة منها على Staging، ثم تعتمد للإنتاج.
3. **اختبار ترحيل الدفع قبل التنفيذ.** يجب التأكد من عدم وجود `payment_reference` مكرر في الفواتير؛ الترحيل سيتوقف عمدًا إذا وجده حتى لا يخفي فسادًا ماليًا.
4. **اختبار عزل محلين فعليًا.** أنشئ محل A ومحل B، وكل دور في كل محل، وحاول الوصول إلى أرقام فروع وقطع وفواتير المحل الآخر. يجب أن تكون النتيجة 403/404 بلا تسريب.
5. **اختبار البيع المتزامن.** نفّذ طلبين على آخر قطعة في الوقت نفسه؛ ينجح طلب واحد فقط ولا يصبح المخزون سالبًا.
6. **إبقاء `PAYMENTS_ENABLED=false`.** لا يفعّل الدفع الحي حتى تمر سيناريوهات Moyasar التجريبية أدناه وتُعتمد تسوية مالية وتشغيل إنذارات الدعم.
7. **تحديد المتطلبات النظامية للفوترة.** إذا كان الإطلاق على منشآت ملزمة بالمرحلة الثانية، فيجب تكامل Fatoora والتوقيع والتبليغ/التخليص قبل الادعاء بالامتثال.

### مهمة قبل التجربة المغلقة

1. ضبط نطاق HTTPS الحقيقي في `CORS_ALLOWED_ORIGINS` وعدم استخدام localhost في الإنتاج.
2. تخزين `DATABASE_URL` و`JWT_SECRET` و`MOYASAR_SECRET_KEY` في مدير أسرار منصة الاستضافة فقط، مع تدوير أي قيمة سبق كشفها.
3. استخدام اتصال Supabase الملائم للخادم المنشور، وضبط `DB_SSL=true` و`DB_POOL_MAX` بحسب حدود الخطة والـPooler.
4. إدخال الاسم القانوني والرقم الضريبي لكل محل قبل السماح بأول عملية بيع.
5. تنفيذ تجربة كاملة: مدير → فرع → دعوة موظف → ربط جهاز → استيراد مخزون → بيع → صرف مستودع → سجل حركة → فاتورة.
6. إعداد مراقبة للأخطاء، تنبيهات الصحة، نسخ احتياطية، وتجربة استعادة فعلية. وجود النسخة الاحتياطية وحده لا يكفي.
7. إعداد سياسة خصوصية وشروط استخدام وحذف/احتفاظ بيانات متوافقة مع نظام حماية البيانات الشخصية السعودي، خصوصًا VIN وبيانات العملاء.
8. إعادة ربط أجهزة المحلات المعتمدة بعد تطبيق `migration_device_security.sql`؛ الرموز القديمة تُلغى عمدًا.
9. إبقاء `PUBLIC_REGISTRATION_ENABLED=false`، وتوفير بصمات SRI الموثقة فقط عند بدء اختبار ميسر.

## سيناريوهات الدفع المطلوبة قبل التفعيل

- بطاقة ناجحة مع 3DS ثم رجوع إلى التطبيق وإنشاء فاتورة واحدة فقط.
- إعادة فتح رابط الرجوع أو إعادة الطلب؛ لا ينشأ خصم أو فاتورة ثانية.
- مبلغ أو عملة أو metadata غير مطابقة؛ يرفض الطلب ولا يسلّم المخزون.
- نفاد المخزون بعد الدفع؛ يحدث استرجاع كامل أو ينشأ تنبيه تسوية يدوي واضح.
- حفظ رمز البطاقة ثم تجديد شهري وسنوي في البيئة التجريبية.
- مهلة اتصال بعد تنفيذ الخصم؛ إعادة المحاولة تستخدم نفس `given_id` ولا تكرر الخصم.
- رفض البطاقة أو انتهاء الرمز؛ تتحول المحاولة إلى الحالة الصحيحة وتبدأ سياسة تذكير ودفع متأخر.
- مطابقة سجل `subscription_payments` و`billing_renewal_attempts` مع لوحة وتسويات Moyasar.

## العيوب والقيود المعروفة

- الاختبارات الحالية تركز على التحقق من القواعد والكود، وليست بديلًا عن Integration/E2E على PostgreSQL حقيقي.
- عزل المستأجرين يعتمد أساسًا على الخادم واستعلاماته؛ أدوار Supabase العامة محجوبة، لكن لا توجد سياسات RLS مستقلة تسمح للمتصفح بالعمل مباشرة. هذا مقصود ما دام المرور عبر API إلزاميًا.
- يوجد تغيير كلمة مرور مع إلغاء الجلسات الأخرى، لكن لا توجد بعد إعادة تعيين موثقة بالبريد أو تحقق بريد إلكتروني أو MFA. هذه مانع إطلاق عام لحسابات المديرين.
- جلسة الويب أصبحت cookie محميًا مع حماية CSRF متعددة الطبقات؛ ما زال يلزم اختبار متصفح E2E على نطاق Staging الحقيقي، خصوصًا عند فصل نطاق الواجهة عن API.
- نموذج ميسر مثبت الإصدار ويفشل دون SRI؛ ما زال اعتماد سلسلة إمداد خارجيًا، ويجب استخراج البصمات من الملفات الرسمية ومراجعتها عند كل تحديث وعدم تجاوز الفشل.
- لا يوجد سجل محاسبي كامل لقيود المرتجعات وأوامر الشراء والتحويلات والتسويات والضرائب المعدّلة والإشعارات الدائنة؛ حركات المخزون للتحويل أصبحت موجودة لكن القيد المالي غير مكتمل.
- لا توجد شاشة إدارة لحالات توافق القطعة (غير موثّق/موثّق/مرفوض) رغم حفظ الحالة في البيانات.
- القوالب السعودية عامة وليست أرقام OEM حقيقية، ولا يجوز تقديمها على أنها قاعدة قطع سعودية شاملة.
- البحث برقم الهيكل يعمل على السيارات التي حُفظت وربطت بتوافقات داخل النظام، وليس خدمة فك VIN عالمية.
- لا توجد اختبارات أداء، سعة، انقطاع شبكة، أو تشغيل دون اتصال لنقاط البيع.
- تطبيق الجوال موجود كمشروع منفصل، ولم تُراجع أو تُثبت مطابقته لنفس عقود API في هذه الجولة.
- لا توجد منصة مراقبة أخطاء وقياسات منتج مفعلة بعد، ولا سياسة حذف تلقائي للسجلات الحساسة.

## الصعوبات المتوقعة مستقبلًا والحلول المقترحة

| التحدي | أثره | الحل المهني المقترح |
|---|---|---|
| اختلاف ملفات Excel بين المحلات | فشل أو أخطاء استيراد | شاشة مطابقة أعمدة، معاينة، دفعات صغيرة، تقرير صفوف مرفوضة، وإمكانية تراجع |
| تكرار أرقام OEM والأسماء | نتائج بحث مضللة | كتالوج مركزي مرخّص، تطبيع العلامات والأرقام، سجل مصادر ودرجة ثقة |
| ترخيص بيانات VIN والتوافق | خطر قانوني وجودة منخفضة | عقد مع مزود بيانات/API مرخص وعدم نسخ مواقع المنافسين |
| مزامنة الموردين والأسعار | بيانات قديمة | Connectors للموردين، جدولة، فروق قابلة للمراجعة، وعدم تعديل سعر المحل تلقائيًا |
| تزاحم البيع بين الفروع والقنوات | نفاد ومبالغة بالمخزون | حجز مؤقت للمخزون، معاملات ذرية، idempotency، وأحداث مخزون قابلة للتسوية |
| المرتجعات والتالف والتسويات | فروق جرد ومالية | بناء مستندات مستقلة وموافقات وتدقيق مزدوج؛ التحويل الأساسي أصبح موثقًا محليًا |
| نمو عدد المحلات | بطء واتصالات كثيرة | فهارس وقياس استعلامات وPooler واختبارات حمل ثم تقسيم عند الحاجة، لا قبلها |
| العمل عند ضعف الإنترنت | توقف الكاشير | PWA/Offline queue بتعارضات واضحة، مع عدم خصم المخزون محليًا بلا مصالحة |
| الامتثال والخصوصية | مخالفة أو فقد ثقة | DPIA، تقليل البيانات، تشفير، سياسات احتفاظ، سجل موافقات واستجابة للحوادث |
| دعم التشغيل | تأخر معالجة أخطاء مالية | معرفات تتبع، سجل تدقيق، لوحة صحة، مناوبة دعم وإجراءات استرجاع موثقة |

## خطة إطلاق آمنة على مراحل

### المرحلة 1 — Staging

1. نسخة قاعدة بيانات تجريبية منفصلة.
2. ترحيل ثم بيانات اختبار غير حقيقية.
3. نجاح CI وE2E والأدوار والأجهزة والمخزون.
4. الدفع متوقف.

### المرحلة 2 — Beta مغلقة

1. محل أو محلان فقط، مستخدمون معروفون، وحدود دعم واضحة.
2. نسخ احتياطية وتنبيهات ومراقبة يومية.
3. الدفع متوقف أو Moyasar test فقط.
4. مراجعة يومية لحركات المخزون والأخطاء.

### المرحلة 3 — دفع تجريبي مضبوط

1. إكمال جميع سيناريوهات Moyasar السابقة.
2. تفعيل العلم لمحل داخلي/تجريبي فقط.
3. مراجعة التسويات والاسترجاعات يدويًا قبل التوسع.

### المرحلة 4 — إطلاق عام تدريجي

1. حل المتطلبات النظامية وZATCA المناسبة لنوع العملاء.
2. اختبارات حمل واستعادة وكشف ثغرات مستقلة.
3. تشغيل على دفعات مع زر إيقاف سريع للدفع والتسجيل العام.

## قائمة اعتماد الإطلاق

- [ ] التغييرات مرفوعة إلى فرع GitHub الصحيح.
- [ ] كل فحوص CI خضراء على نفس SHA المراد نشره.
- [ ] نسخة Staging مطابقة للإنتاج.
- [ ] نسخة احتياطية واستعادة مجرّبة.
- [ ] الترحيلات نجحت على Staging دون تعديل ترحيل سبق تطبيقه.
- [ ] اختبار محلين وجميع الأدوار والأجهزة نجح.
- [ ] اختبار الاستيراد والبيع المتزامن والصرف والمرتجع نجح.
- [ ] نطاقات HTTPS وCORS وأسرار الاستضافة صحيحة.
- [ ] سياسات الخصوصية والشروط والدعم منشورة.
- [ ] ZATCA مناسبة لنطاق الإطلاق.
- [ ] الدفع بقي متوقفًا أو اجتاز اعتماد Moyasar الكامل.
- [ ] المراقبة والتنبيهات وخطة التراجع تعمل.

## الخلاصة

المشروع تطور من نموذج أولي إلى أساس جيد لتجربة ويب مغلقة: العزل والصلاحيات والمخزون والتدقيق والدعوات والأجهزة وحفظ السيارات والبحث والدفع أصبحت أكثر نضجًا. لكن علامة «جاهز للإطلاق العام» لا تمنحها اختبارات الوحدات وحدها. البوابات الحاسمة المتبقية هي بناء CI على النسخة المرفوعة، ترحيل Staging واختبار PostgreSQL الفعلي، تجربة مستخدم كاملة، مراقبة تشغيلية، واعتماد المدفوعات والامتثال.
# تحديث الامتثال السعودي — 13 أغسطس 2026

- أضيفت لوحة امتثال داخلية للمدير وسياسة خروج شبكي مقيدة.
- أضيف أساس آمن لاتصال ZATCA Reporting، لكنه **متوقف افتراضيًا وغير جاهز للإنتاج** حتى اكتمال UBL والتوقيع وCSID والاختبارات الرسمية.
- أضيف سجل محاولات حكومية لا يخزن XML أو بيانات العميل أو أسرار الاتصال.
- أضيف رقم السجل التجاري لملف المنشأة.
- التفاصيل وخطة العمل: [SAUDI_COMPLIANCE_AND_GOVERNMENT_INTEGRATION_AR.md](SAUDI_COMPLIANCE_AND_GOVERNMENT_INTEGRATION_AR.md).

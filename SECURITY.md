# سياسة أمن مشروع ركائز

## الإبلاغ المسؤول

لا تُفتح ثغرة تتضمن بيانات عملاء أو مفاتيح أو طريقة استغلال كاملة في Issue عامة.
يرسل البلاغ سرًا إلى مالك المشروع، ويتضمن النسخة المتأثرة، الأثر المتوقع، وخطوات
إعادة آمنة على بيانات اختبار. لا تختبر على Supabase الإنتاجي ولا تصل إلى بيانات
محل لا تملكه.

## الأسرار

- لا يوضع `DATABASE_URL` أو `JWT_SECRET` أو `MOYASAR_SECRET_KEY` في Git أو الواجهة.
- المفتاح الوحيد المسموح في بناء الويب هو مفتاح ميسر القابل للنشر `pk_*`.
- أي مفتاح ظهر في صورة أو سجل أو Commit قديم يعامل على أنه مكشوف ويدوّر من مزوده.
- تستخدم منصة النشر مدير أسرار وصلاحيات أقل قدر ممكن، مع فصل Staging عن Production.

## التحديث والاستجابة

- يراجع Dependabot أسبوعيًا، ويمنع الإطلاق عند وجود ثغرة متوسطة فأعلى في حزم
  الإنتاج أو ثغرة عالية فأعلى في شجرة التطوير، حتى تقييمها أو إصلاحها.
- تحفظ سجلات التطبيق بدون كلمات مرور أو رموز بطاقات أو JWT، ويستخدم `X-Request-ID`
  لربط البلاغ بالسجل.
- عند حادث أمني: يوقف الدفع إن كان مرتبطًا، تعزل النسخة المتأثرة، تدوّر الأسرار،
  تحفظ الأدلة، تراجع نطاق البيانات، ثم تستعاد الخدمة من نسخة موثوقة بعد الاختبار.

## نطاق الضمان

هذه الضوابط تقلل المخاطر ولا تضمن منع كل اختراق. أي إطلاق عام يحتاج اختبار اختراق
مستقل، مراقبة تشغيلية، وخطة استجابة وتمارين استعادة دورية.
